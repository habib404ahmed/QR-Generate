// Firebase removed — database is now MySQL (backend only).
// This file is kept as a stub so imports don't break.
// Real-time updates are handled via API polling in the hooks.

export const db = null;
export const collection = null;
export const doc = null;
export const onSnapshot = null;
export const query = null;
export const orderBy = null;

export default null;
